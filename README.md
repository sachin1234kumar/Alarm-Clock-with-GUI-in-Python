# Alarm-Clock-with-GUI-in-Python
The project creates a GUI-based Alarm Clock in Python using Tkinter, datetime, and time modules. It allows users to set a specific time, and when the current time matches, an alarm sound is triggered. The project demonstrates basic GUI development and time handling in Python.
